#use <conio>

     int factorial(int n) {
       if (n == 0) return 1;
       else return n * factorial(n-1);
}
int main () {
       printint(factorial(11));
       println(" is the factorial of 11");
       return 0;
}
